<div class="container">
   <div class="top-page">
     <h1>Suits</h1>
      <div class="navigation">
        <p><%= link_to 'New Suit', new_suit_path, class: "btn btn-orange" %></p>
      </div>
   </div>

  <div class="pagination">
    <%== pagy_bootstrap_nav(@pagy).html_safe %>
  </div>

  <div class="container d-inline">
    <div class="cards">
      <% @suits.each do |suit| %>
        <div class="card">
        <%= link_to suit do %>
           <div class="card-body">
            <% if suit.photos.first %>
              <%= cl_image_tag suit.photos.first.key, class: "card-img-top" %>

            <% else %>
               <img src="https://images.unsplash.com/photo-1572325879775-5e8f8b20439f?ixid=MXwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHw%3D&ixlib=rb-1.2.1&auto=format&fit=crop&w=634&q=80" class="card-img-top">
              <% end %>

            <h5><%= suit.name %></h5>
              <div id="card-details">
              <p>£<%= suit.price %> per day</p>
              <p><%= suit.brand %></p>
              <p>Size: <%=suit.size %> </p>
            </div>
          </div>
          <% end %>
        </div>
      <% end %>
    </div>
    <!-- <table class="table">
      <thead>
        <tr>
          <th>Name</th>
          <th>Brand</th>
          <th>Price</th>
          <th>Style</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @suits.each do |suit| %>
          <tr>
            <td><%= suit.name %></td>
            <td><%= suit.brand %></td>
            <td><%= suit.price %></td>
            <td><%= suit.style  %></td>
            <td><%= link_to 'Show', suit %></td>
            <td><%= link_to 'Edit', edit_suit_path(suit) %></td>
            <td><%= link_to 'Destroy', suit, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table> -->
  </div>
<div class="pagination">
  <%== pagy_bootstrap_nav(@pagy).html_safe %>
</div>

  <div class="navigation">
    <p><%= link_to 'New Suit', new_suit_path, class: "btn btn-orange" %> <%= link_to root_path, class: "btn btn-gray" do%> <i class="fas fa-arrow-circle-left"></i><span> Back to homepage</span>
    <% end %> </p>
  </div>
</div>
